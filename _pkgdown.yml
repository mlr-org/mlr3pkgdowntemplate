template:
  package: mlr3pkgdowntemplate
  params:
    bootswatch: journal

development:
  destination: dev
  version_label: default
  version_tooltip: "Development version"

home:
  strip_header: true
